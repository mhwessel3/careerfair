import React from "react";
import MainContent from "./MainContent";

class InterviewLocations extends React.Component {
  constructor(props) {
    super(props);
  }

  render() {
    return <div className="memberCards" />;
  }
}

export default InterviewLocations;
